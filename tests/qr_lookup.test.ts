// qr_lookup.test.ts — scaffold
import request from 'supertest';
import express from 'express';

// This test is a scaffold. The Next.js API routes can be tested via supertest by importing the handler
// and wrapping it in a small express app. Alternatively, test the modules directly with mocked supabase client.

describe('QR lookup API', () => {
  it('should return 404 for unknown code (scaffold)', async () => {
    // TODO: wire handler and mock supabase client
    expect(true).toBe(true);
  });
});
